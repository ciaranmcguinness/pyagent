[project]
name = "pyagent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dill>=0.4.1",
    "openai-agents[any-llm]>=0.17.6",
    "pexpect>=4.9.0",
]
